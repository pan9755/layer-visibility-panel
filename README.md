# Layer Visibility Panel (Frontend)

Simple Vite + React + TypeScript app demonstrating a CAD-style layer panel.

## Setup

1. Clone or create folder `layer-visibility-panel` and paste files.
2. Install:
   ```bash
   npm install
